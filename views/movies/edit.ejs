<!DOCTYPE html>
<html>
  <head>
    <title> <%= title %> </title>
    <% include ../partials/head.ejs %>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="row ">
      <h2 class="col s10 offset-s1">Edit movie: <%= movie.title %></h2>
    </div>
    <div class="row">
      <form class="col s12" action="<%= '/movies/' + movie.id + '?_method=PUT' %>" method="POST">
      <div class="row">
        <div class="col s12">
          <label>Title:</label>
          <input value="<%= movie.title %>" name="title" placeholder=' <%= movie.title %>' />
        </div>
      <div class="row">
        <div class="col s12">
          <label>Synopsis:</label>
          <input value="<%= movie.synopsis %>" name="synopsis" placeholder= '<%= movie.synopsis %>'></input>
        </div>
      </div>
        <button class="btn" type='submit'>submit</button>
      </div>
      </form>
    </div>
  </body>
</html>
