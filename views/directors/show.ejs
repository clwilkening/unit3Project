<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= director.name %></title>
  <% include ../partials/head.ejs %>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
<div class="row">
        <div class="col s10 offset-s1">
          <div class="card blue-grey lighten-1">
            <div class="card-content white-text">
              <span class="card-title"><h3><%= director.name %></h3></span>
            </div>
            <div class="card-action">
            <div class="row">
              <a href='<%= `/directors/${director.id}/edit` %> '><button class="btn">Edit</button></a>
              <form action='<%= `/directors/${director.id}?_method=DELETE` %>' method='POST'>
                <button class="btn" type="submit">X</button>
              </form>
              <a href="/directors"><button class="btn">Back</button></a>
            </div>
          </div>
        </div>
      </div>
</body>
</html>
